# 【引継書・共有用】Personalized Listening Trainer（中国語ピンイン特化リスニングトレーナー）

本ドキュメントは、別スレッドや他のAIエージェント、共同開発者に対して本プロジェクトの**思想、達成事項、アーキテクチャ、設計仕様、ノウハウ（ハマりどころと解決策）、今後の拡張案**を短時間で正確に共有するためのハンドオーバー資料です。

---

## 1. プロジェクト概要

- **名称**: Personalized Listening Trainer（確定版ミニマルモデル）
- **対象**: 中国語（HSK 1〜3級レベルのセンテンス × 全40文収録）
- **実行環境**: 完全ローカル動作（サーバー不要、各種モダンブラウザ対応、GitHub Pages対応）
- **技術スタック**: Vanilla HTML5 / CSS3 / JavaScript (ES6+), Web Speech API (ブラウザ標準TTS)

### 開発思想
1. **「聴覚の同期」に100%特化**:
   発音（筋肉の運動）は対象外とし、日本人の脳内にある「ローマ字知識」とネイティブの「リアルな現地音」のミスマッチを破壊することに集中する。
2. **左手1本のLazy-UI**:
   キーボードのホームポジション（左手のみ）で全学習フローが完結し、視線は画面（ピンイン・漢字）に完全固定できる。
3. **ゼロ・インストールのWebネイティブ**:
   ビルドツールやバックエンドサーバーを一切使わず、静的ファイル（`trainer.html` + `data.js`）のみで完結。

---

## 2. 実装済み機能・UI仕様

### 2.1 学習フローとモード設計
| モード | 発動キー | 挙動詳細 |
|---|---|---|
| **Challenge Mode（回答前）** | 初期状態 / `E` | 漢字・ピンインを伏せ（`q-hidden`）、音声のみ再生。チャンク区切りの下線のみ表示。 |
| **Answer Mode（答え合わせ）** | `Q` | 漢字・ピンイン、日本語訳、発音Tipsを一斉開示。文全体を等倍（1.0x）で自動再生。 |
| **Re-Challenge（再挑戦）** | `E` | 開示状態から即座にChallenge状態へワンタッチリセット。同一文の繰り返し訓練を支援。 |
| **Fモード（お助け再生）** | `F` | 文をチャンクごとに分割し、**800msのポーズ**を挟みながら順次再生。境界スラッシュ `/` を点灯。答え合わせ前後問わず利用可能。 |
| **Wモード（単語ヒント）** | `W` | HSK設定（2級以上 / 3級以上）に該当する単語の意味バッジをトグル表示。 |
| **Gモード（ピンイン爆撃）** | `G` | 答え合わせ後に有効。文中の「天敵ピンイン」単語を順次巡回フォーカス。<br>・ピンインが巨大化（2.8rem）＋薄黄色座布団でハイライト。<br>・該当単語を **0.7x → 0.85x → 1.0x（インターバル250ms）で加速3連打再生**。<br>・下部に弱点解説カードを表示（複数天敵がある単語はカードを並列表示）。<br>・最後まで押すとGモードを解除して通常文再生に復帰するループ設計。 |
| **シャッフルモード** | `S` | 全40文をFisher-Yates法でランダム出題（**デフォルトでON**。SキーUIが緑点灯）。 |

### 2.2 キーボードマッピング（左手完結）
- `Space` / `R`: 音声再生（Challenge時: 設定速度 / Answer時: 1.0x / Gモード中: 対象語等倍）
- `Q`: 答え合わせ（開示）/ Gモード解除
- `E`: 再チャレンジ（伏せ状態にリセット）
- `F`: お助けスロー分割再生（800msディレイ）
- `W`: 単語ヒントトグル
- `G`: ピンイン爆撃（加速3連打＆天敵解説）
- `S`: シャッフルON/OFF切り替え
- `1` / `2` / `3`: 速度ギア（-0.1x / 1.0x等倍リセット / +0.1x）
- `A` / `4` / `←`: 前の文へ
- `D` / `5` / `→`: 次の文へ
- **自動再生**: ページ読み込み時および文移動（`A`/`D`）時に次問を自動発音。

---

## 3. 天敵ピンイン分類システムとCSSカラー定義

日本人学習者が脳内でバグを起こしやすい12大天敵ピンインをカテゴリ化し、色とアンダーラインで視覚化：

| クラス名 | カテゴリ | 表示色 (ダーク背景 / Gモード座布団上) | 対象音素の例 |
|---|---|---|---|
| `.bad-an` | ローマ字/省略の罠 | ネオンオレンジ / 暗いオレンジ (`#b35c00`) | `-ian` (イェン), `-ui` (ウェイ), `-un` (ウェン) |
| `.bad-e` | こもる『e』の罠 | ネオンレッド / 暗い赤 (`#b81d1d`) | `e` (喉の奥のこもったウ／オ) |
| `.bad-retro` | 巻き舌・無気音の罠 | ネオンブルー / 暗い青 (`#0b59b3`) | `zh`, `ch`, `sh`, `r`, 空母音 `zi/si`, `j` |
| `.bad-ing` | 鼻音終わりの罠 | ネオングリーン / 暗い緑 (`#0a6b3f`) | `-ng` (鼻に抜けるン) |
| `.bad-front` | 前舌面の罠 | ネオンパープル / 暗い紫 (`#6b21a8`) | `x-` (`xué`, `xí`, `xū`, `xīng` 等) |

### 複合トラップ（コンボ）の表現
1音節に子音と母音の複数罠がある場合（例: `常 cháng` ＝ 巻き舌 + 鼻音、`写 xiě` ＝ 前舌面 + -ian）:
- **文字色**: 子音属性（巻き舌=青、前舌面=紫）
- **下線（破線）**: 母音属性（鼻音=緑、-ian=橙、こもるe=赤）
- これにより、1文字の中で2つの罠を直感的に識別可能。

---

## 4. ファイル構成とアーキテクチャ

```text
c:\Antigravity\Projects\ListeningTraining\
├── trainer.html       # メインSPA（UI、CSS、キーイベント、音声再生制御ロジック）
├── data.js           # 12大天敵DBマスター + 演習データ（全40文、2200行超）
├── README.md         # プロジェクト仕様書
└── PROJECT_HANDOVER.md # 本引継ぎドキュメント
```

### `data.js` のデータスキーマ
```javascript
{
  "full_sentence": "他学了两年中文，说得非常好。",
  "japanese_translation": "彼は2年間中国語を学んで、とても上手に話せます。",
  "tip": "学(xué)の前舌音、年(nián)の『イェン』省略、中(zhōng)の巻き舌＋鼻音...",
  "html_content": `...<div class="chunk-box" data-chunk-index="0">...`, // .char-boxの垂直Flex構造
  "chunks": [
    { "text": "他学了两年", "words": [{"word": "学", "level": 1, "meaning": "学ぶ"}, ...] },
    ...
  ],
  "vulnerable_targets": [
    {
      "text": "学",
      "enemy_id": "ROM-ü,VOW-xi",
      "html": `<div class="char-box"><span class="pinyin bad-front">xué</span><span class="hanzi">学</span></div>`
    },
    ...
  ]
}
```

---

## 5. 解決済みの技術課題・ノウハウ（重要）

1. **Gモード座布団のシームレス結合**:
   - 複数文字の単語（例: `非常`）で個別ボックスに `transform: scale` や角丸を当てると、カード同士が重なり継ぎ目が汚れる問題。
   - **解決策**: 個別の `scale` を廃止し、`margin: 0`。JavaScriptで単語先頭文字に `.focus-start`（左角丸）、末尾文字に `.focus-end`（右角丸）を動的付与し、**1枚の滑らかなピル型カプセルカード**として結合表示。
2. **部分文字列衝突によるGモード不達問題**:
   - 例: `去电影院` と `看电影` が同一文内にある場合、ターゲット名を `电影` にしていると手前の `电影院` に誤マッチして末尾の `看电影` にフォーカスが届かない。
   - **解決策**: ターゲット名を chunk と一致する一意な `看电影` に設定し、前方一致の衝突を排除。
3. **Web Speech API のゾンビ再生・競合防止**:
   - 非同期タイマー連打やキー割り込みで音声が重なるバグ。
   - **解決策**: 音声停止時に `speechSynthesis.cancel()` だけでなく、全タイマーID配列 `activeTimeouts` を一括 `clearTimeout`。さらに `playGBombardment` 開始時の `snapshotGIndex` を保持し、割り込み発生時に後続コールバックを即座に無効化。
4. **ブラウザAutoplay制限対策**:
   - ページ起動時にユーザー操作なしで音声がブロックされる問題に対し、`init()` 時にダミー発音をキューイングしてアンロックに備える設計。

---

## 6. 今後の推奨拡張アイデア（Next Steps）

1. **学習進捗・苦手トラップのローカル保存（LocalStorage）**:
   - 文ごとの理解度（1: 要復習, 2: あいまい, 3: 完璧）やGモード実行回数を記録し、「苦手天敵ピンイン集中モード」を実現。
2. **天敵解説カードの日本語音声読み上げ**:
   - Gモード突入時、画面下の解説文を `ja-JP` 音声で読み上げ、視線を文字から外さずに聴覚だけでインプット。
3. **文数の追加（60〜80文目標）**:
   - 現在40文。全12大天敵のあらゆる組み合わせを網羅する理想ライン（60〜80文）に向けたデータ拡充。
4. **自動パーサー/データジェネレーター**:
   - 中国語テキストを入力すると、pinyin変換と12大天敵タグ（`.bad-*`）および chunk を自動推定して `data.js` 形式を出力するスクリプトの整備。
